<!-- markdownlint-disable MD007 -->
您正在分析编码代理 (Claude Code) 的技能/规则文件。
您的任务：提取该技能激活时代理应遵循的**可观察的行为序列**。

每个步骤都应该用自然语言描述。不要使用正则表达式模式。

仅以这种精确格式输出有效的 YAML（无降价栅栏，无注释）：

id: <kebab-case-id>
name: <人类可读的名称>
source_rule: <提供的文件路径>
版本：“1.0”

步骤：
  - id：<snake_case>
    描述：<代理应该做什么>
    必填：真|假
    探测器：
      描述：<调用要查找的工具的自然语言描述>
      after_step: <step_id 这必须在后面，可选 — 如果不需要则省略>
      before_step: <step_id 这必须出现在之前，可选 — 如果不需要则省略>

得分：
  提升至挂钩阈值：0.6

规则：
- detector.description 应描述工具调用的含义，而不是模式
  好：“编写或编辑测试文件（不是实现文件）”
  错误：“写入|使用输入匹配测试进行编辑。*\\.py”
- 对于 ORDER 很重要的技能，使用 before_step/after_step（例如 TDD：在 impl 之前进行测试）- 忽略只有“存在”才重要的技能的排序限制
- 将步骤标记为必需：仅当技能说“可选”或“如果适用”时为 false
- 3-7 步是理想的。不要过度分解
- 重要提示：用双引号引用所有包含冒号的 YAML 字符串值
  好：描述：“使用常规提交格式（类型：描述）”
  坏：描述：使用常规提交格式（类型：描述）

技能文件分析：

---
{技能内容}
---